<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hackingproject.signup.mapper.SignupMapper">
    
    <insert id="insertUser" parameterType="com.example.hackingproject.signup.userentity.UserEntity">
        INSERT INTO users (userIdNo, userPw, userTelno, userName, userBirth, userBank, accountNumber, userAgency, accessLevel, accountBalance)
        VALUES (#{userIdNo}, #{userPw}, #{userTelno}, #{userName}, #{userBirth}, #{userBank}, #{accountNumber}, #{userAgency}, #{accessLevel}, #{accountBalance})
    </insert>

</mapper>
